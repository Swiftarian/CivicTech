# 部署指南

## 📦 方案一：Windows 本機部署（推薦給非技術人員）

### 前置需求
- Windows 10/11
- Python 3.9 或以上版本
- Tesseract OCR（用於 OCR 功能）

### 安裝步驟

1. **安裝 Python**
   - 下載：https://www.python.org/downloads/
   - 安裝時勾選「Add Python to PATH」

2. **安裝 Tesseract OCR**
   - 下載：https://github.com/UB-Mannheim/tesseract/wiki
   - 預設安裝路徑：`C:\Program Files\Tesseract-OCR\`
   - 安裝繁體中文語言包

3. **安裝專案依賴**
   ```bash
   pip install -r requirements.txt
   ```

4. **啟動系統**
   - 方法 1：雙擊 `run.bat` 一鍵啟動
   - 方法 2：手動執行 `streamlit run 首頁.py`

5. **訪問系統**
   - 預設網址：http://localhost:8501
   - 預設管理員帳號：`admin` / `admin123`

---

## 🐳 方案二：Docker 容器化部署（推薦給伺服器）

### 前置需求
- Docker
- Docker Compose

### 快速啟動

```bash
# 1. 啟動服務
docker-compose up -d

# 2. 查看 logs
docker-compose logs -f

# 3. 停止服務
docker-compose down
```

### 訪問系統
- 網址：http://<伺服器IP>:8501
- Port 可在 `docker-compose.yml` 修改

---

## ⚙️ 環境變數設定

建立 `.streamlit/secrets.toml`（請勿上傳至 Git）：

```toml
[email]
sender_email = "your-email@gmail.com"
sender_password = "your-app-password"
```

---

## 🔐 安全性建議

1. **修改預設密碼**
   - 首次部署後立即修改 admin 密碼
   - 使用強密碼（英數混合 + 特殊字元）

2. **設定防火牆**
   - 限制 8501 Port 僅對內網開放
   - 或使用 Nginx 反向代理 + SSL

3. **定期備份**
   - 資料庫：`cases.db`
   - 上傳檔案：`uploads/`
   - 系統資料：`downloads/`

---

## 📊 系統維護

### 查看系統紀錄
- 登入後選擇「系統紀錄」（管理員限定）

### 資料庫備份
```bash
# Windows
copy cases.db cases_backup_20250122.db

# Linux/Mac
cp cases.db cases_backup_$(date +%Y%m%d).db
```

### 更新系統
```bash
git pull
pip install -r requirements.txt --upgrade
```

---

## 🆘 常見問題

### Q: 無法啟動？
- 檢查 Python 版本：`python --version`
- 檢查依賴安裝：`pip list`
- 查看錯誤訊息

### Q: OCR 無法使用？
- 確認 Tesseract 已安裝
- 檢查路徑設定（側邊欄 OCR 設定）

### Q: Email 無法發送？
- 檢查 `secrets.toml` 設定
- Gmail 需要使用「應用程式密碼」而非帳號密碼

---

## 📞 技術支援

- 系統問題：請聯繫系統管理員
- 帳號問題：使用「忘記密碼」功能或聯繫管理員
